{
  "name": "voteflux",
  "version": "1.0.0",
  "description": "Flux website",
  "main": "index.js",
  "scripts": {
    "flux": "parallelshell 'gulp' 'jekyll build --watch'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David Factor",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^6.3.6",
    "browser-sync": "^2.11.2",
    "concurrently": "^2.0.0",
    "gulp": "^3.9.1",
    "parallelshell": "^2.0.0"
  }
}
